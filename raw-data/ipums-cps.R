library(tidyverse)
library(ipumsr)
library(survey)
library(srvyr)

# below is an example, I will need to download and read in the files myself.
#cps_ddi_file <- ipums_example('cps_00158.xml')
# cps_data <- read_ipums_micro(cps_ddi_file)
